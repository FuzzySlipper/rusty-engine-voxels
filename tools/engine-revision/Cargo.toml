[package]
name = "rusty-engine-voxels-revision"
version = "0.1.0"
edition = "2021"
rust-version = "1.89"
description = "Rusty Engine development resolution and exact certification commands for the voxel lab"
license = "MIT"

[workspace]

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
pedantic = "warn"
